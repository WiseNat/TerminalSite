This site provides the typical command history to improve QoL when using the terminal.

This means that previous commands can be accessed and modified with these modifications being saved.
To scroll along the command history, use the `Arrow Up` and `Arrow Down` keys.
