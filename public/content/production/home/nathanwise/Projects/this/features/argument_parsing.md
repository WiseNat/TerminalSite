All commands typically rely on a 'getopts'-like library for parsing arguments.
This means that typical complex command argument parsing is supported for the majority of commands.

For example:
- `ls -lsa`
- `terminal --theme=Dark`
- `tree --prune / -L 3 -ad`
